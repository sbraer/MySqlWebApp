@page
@model PrivacyModel
@{
    ViewData["Title"] = "Privacy Policy";
}
<h1>@ViewData["Title"]</h1>

<p>Use this page to detail your site's privacy policy.</p>

@if (!string.IsNullOrEmpty(Model.ClaimInfo))
{
    @Html.Raw(Model.ClaimInfo)
}